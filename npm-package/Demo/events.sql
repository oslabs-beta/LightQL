create table simple_events (
  id text,
  name text,
 start_date text,
 primary_venue_id text,
 summary text,
 image text,
 value integer,
 primary_venue text,
 address text,
 city text,
  ticket_available boolean
);